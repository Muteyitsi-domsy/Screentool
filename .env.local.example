# ─── Supabase ────────────────────────────────────────────────────────────────
# From: supabase.com → Project → Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ─── Paddle Billing ───────────────────────────────────────────────────────────
# From: Paddle dashboard → Developer Tools → Authentication → Client-side token
VITE_PADDLE_CLIENT_TOKEN=

# Price IDs from Paddle dashboard → Catalog → Prices
VITE_PADDLE_PRICE_ID_SUBSCRIPTION=
VITE_PADDLE_PRICE_ID_LIFETIME=

# Use "sandbox" for local dev, "production" for live
VITE_PADDLE_ENVIRONMENT=sandbox

# ─── Supabase Edge Function secrets (set via CLI, NOT here) ──────────────────
# supabase secrets set PADDLE_WEBHOOK_SECRET=xxx
# SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are auto-injected by Supabase.
